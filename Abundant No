#include<iostream.h>
using namespace std;
int main()
{
int n=12,sum=0;
for(int i=1;i<=n;i++)
  {
if(n%i==0)
sum=sum+i;
  }
if(sum>n)
 {
cout<<n<<"It is abudant number";
cout<<"The abundance is :"<<(sum-n);
  }
else
cout<<"It is not a abundant number";
}
